var t = TrelloPowerUp.iframe();

t.render(async function() {
  document.body.innerHTML = "<p>Copying list…</p>";
  // For real implementation, this popup would collect sourceListId and targetBoardId
});
